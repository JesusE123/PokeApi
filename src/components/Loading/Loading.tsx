const Loading = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-full flex items-center justify-center  z-50">
      <img src="/public/pikachu.gif" width={250} height={250} />
    </div>
  );
};

export default Loading;
